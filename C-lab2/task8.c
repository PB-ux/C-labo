#include <stdio.h>

int main(){
    int a = 1,b = 2, c = 3,e,f;
    e = 2;
    f = 5;
    if (a >= 2 && a <= f)
        printf("%d\n",a);
    if (b >= 2 && b <= f)
        printf("%d\n",b);
    if (c >= 2 && c <= f)
        printf("%d\n",c);
    return 0;
}
